package com.example.my_kmp_project.app

import androidx.compose.runtime.Composable

/**
 * Entry alias for the product shell ([AppShell]).
 * Kept so existing call sites continue to compile.
 */
@Composable
internal fun DemoApp() {
    AppShell()
}
